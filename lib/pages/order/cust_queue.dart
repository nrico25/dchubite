import 'package:flutter/material.dart';

class CustomerQueue extends StatelessWidget {
  const CustomerQueue({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}